import { ModifierCeilPipe } from './modifier-ceil.pipe';

describe('ModifierCeilPipe', () => {
  it('create an instance', () => {
    const pipe = new ModifierCeilPipe();
    expect(pipe).toBeTruthy();
  });
});
