import { Modifier5ePipe } from './modifier-5e.pipe';

describe('Modifier5ePipe', () => {
  it('create an instance', () => {
    const pipe = new Modifier5ePipe();
    expect(pipe).toBeTruthy();
  });
});
