import { CharacterBaseplate } from './character-baseplate';

describe('CharacterBaseplate', () => {
  it('should create an instance', () => {
    expect(new CharacterBaseplate()).toBeTruthy();
  });
});
